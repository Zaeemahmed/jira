{
  "private": true,
  "scripts": {
    "start": "lerna run  --parallel start",
    "build": "lerna run build",
    "serve": "lerna run --parallel serve",
    "clean": "lerna run --parallel clean",
    "install": "lerna run --parallel install"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "name": "jira"
}
